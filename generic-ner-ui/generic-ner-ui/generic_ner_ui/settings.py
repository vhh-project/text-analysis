"""
Django settings for generic_ner_ui project.

Generated by 'django-admin startproject' using Django 2.1.4.

For more information on this file, see
https://docs.djangoproject.com/en/2.1/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/2.1/ref/settings/
"""

import os
from typing import Sequence

from django.utils.log import DEFAULT_LOGGING

from generic_ner_ui.static import config

DEFAULT_LOGGING['handlers']['console']['filters'] = []
# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/2.1/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '*962%+m+4usv+kvqyc-e%^@ymtbu75u7h&uc*!-kd6h)1^nfqi'

# SECURITY WARNING: don't run with debug turned on in production!
# DEBUG = True
DEBUG = os.environ.get("DEBUG", "TRUE") == "TRUE"

ALLOWED_HOSTS = ['*']

# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.sites',
    'django.contrib.staticfiles',
    'bootstrap4',
    'social_django',
    'django_icons',
    #'generic_ner_ui',
    'main.apps.MainConfig',
    'django_global_request',
    'django_static_jquery_ui',
]
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'django.contrib.sites.middleware.CurrentSiteMiddleware',
    'django_global_request.middleware.GlobalRequestMiddleware',
]

if DEBUG:
    INSTALLED_APPS.append("livereload")
    MIDDLEWARE.append("livereload.middleware.LiveReloadScript")

SITE_ID = 1

ROOT_URLCONF = 'generic_ner_ui.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, "templates")],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'generic_ner_ui.wsgi.application'

AUTHENTICATION_BACKENDS = (
    'social_core.backends.keycloak.KeycloakOAuth2',
    'django.contrib.auth.backends.ModelBackend',
)

# Database
# https://docs.djangoproject.com/en/2.1/ref/settings/#databases

DATABASES = {
    'default': {
        'OPTIONS': {
            'charset': 'utf8mb4',
        },
        'ENGINE': 'django.db.backends.mysql',
        'NAME': config.database.schema,
        'USER': 'root',
        'PASSWORD': 'root1',
        'HOST': '127.0.0.1',
        'PORT': config.database.port,
    }
}


        # 'USER': 'root',
        # 'PASSWORD': 'root1',
        # 'HOST': '127.0.0.1',

SECRET = "5IYGRyR5cGi8TJjTzxg6NfJrZVV1hkvoEKxSfGlj"
ALLOWED_IPS = ['127.0.0.1', '127.0.0.2', '']

# Password validation
# https://docs.djangoproject.com/en/2.1/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS: Sequence[str] = []

# Internationalization
# https://docs.djangoproject.com/en/2.1/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True

# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/2.1/howto/static-files/

STATIC_URL = '/static/'

STATICFILES_DIRS = [
    os.path.join(BASE_DIR, "static"),
]

FORCE_SCRIPT_NAME = os.environ.get('SCRIPT_NAME', '')

LOGIN_URL = "/login/keycloak"
LOGIN_REDIRECT_URL = '/'
LOGOUT_REDIRECT_URL = '/'

if FORCE_SCRIPT_NAME:
    LOGIN_URL = f'{FORCE_SCRIPT_NAME}{LOGIN_URL}'
    LOGIN_REDIRECT_URL = f'{FORCE_SCRIPT_NAME}{LOGIN_REDIRECT_URL}'
    LOGOUT_REDIRECT_URL = f'{FORCE_SCRIPT_NAME}{LOGOUT_REDIRECT_URL}'

STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')
STATICFILES_STORAGE = 'whitenoise.storage.CompressedStaticFilesStorage'

SOCIAL_AUTH_POSTGRES_JSONFIELD = True

SOCIAL_AUTH_KEYCLOAK_KEY = config.keycloak.key
SOCIAL_AUTH_KEYCLOAK_SECRET = config.keycloak.secret
SOCIAL_AUTH_KEYCLOAK_PUBLIC_KEY = config.keycloak.public_key
SOCIAL_AUTH_KEYCLOAK_ID_KEY = config.keycloak.id_key

protocol = config.keycloak.protocol
host = config.keycloak.host
access_host = config.keycloak.access_host or host
realm = config.keycloak.realm
SOCIAL_AUTH_KEYCLOAK_AUTHORIZATION_URL = f"{protocol}://{host}/auth/realms/{realm}/protocol/openid-connect/auth"  # noqa: E501
LOGOUT_URL = f"{protocol}://{host}/auth/realms/{realm}/protocol/openid-connect/logout?redirect_uri="  # noqa: E501
if host != access_host:
    protocol = "http"

SOCIAL_AUTH_KEYCLOAK_ACCESS_TOKEN_URL = f"{protocol}://{access_host}/auth/realms/{realm}/protocol/openid-connect/token"  # noqa: E501

SOCIAL_AUTH_URL_NAMESPACE = 'social'

DJANGO_ICONS = {
    "ICONS": {
        "upload": {"name": "upload"},
        "error": {"name": "exclamation-triangle"},
        "success": {"name": "check"},
        "times": {"name": "times"},
        "progress": {"name": "spinner"},
        "ner": {"name": "list-ul"},
        "text": {"name": "align-justify"},
        "ocr": {"name": "columns"},
        "document": {"name": "picture-o"},
        "delete": {"name": "trash"},
        "ner-stats": {"name": "bar-chart"},
        "download": {"name": "file-pdf-o"},
        "download_file": {"name": "download"},
        "back": {"name": "chevron-left"},
        "compress": {"name": "compress"},
        "waiting": {"name": "clock-o"},
        "lightbulb": {"name": "lightbulb-o"},
        "file-text": {"name": "file-text-o"},
    },
}

DATA_UPLOAD_MAX_NUMBER_FIELDS = 2000

pipeline_names = list(map(lambda p: p.name, config.pipelines.pipelines))
assert len(pipeline_names) == len(set(pipeline_names))

for i, pipe in enumerate(config.pipelines.pipelines):
    if i == 0:
        assert len(pipe.depends_on) == 0
        assert pipe.return_pages
    else:
        assert len(pipe.depends_on) != 0
        assert not pipe.return_pages
